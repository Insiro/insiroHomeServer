spring:
  config:
    import: "file:./data/env.yml"
  datasource:
    url: ${DB.url}
    username: ${DB.username}
    password: ${DB.password}
    driver-class-name: org.h2.Driver
  exposed:
    generate-ddl: true
server:
  error:
    include-stacktrace: never
